#include <stdio.h>
/**
 * main - prints the name of the file
 * @argc: argument counter
 * @argv: agument array
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
